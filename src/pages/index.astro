---
import Layout from '../layouts/Layout.astro';
import Footer from '../components/Footer.astro';
---

<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Inverzzo: La plataforma #1 para inversionistas minoristas en Colombia. Únete a la lista de espera para acceder a datos confiables y simulaciones de inversión.">
  <meta name="keywords" content="Inverzzo, inversiones Colombia, herramientas de inversión, CDTs, acciones BVC, educación financiera">
  <meta name="author" content="Inverzzo">
  <meta property="og:title" content="Inverzzo - Plataforma #1 para Inversionistas Minoristas">
  <meta property="og:description" content="Únete a Inverzzo y toma el control de tus inversiones con datos confiables y simulaciones de portafolios.">
  <meta property="og:image" content="https://inverzzo.com/og-image.jpg">
  <meta property="og:url" content="https://inverzzo.com">
  <meta property="og:type" content="website">
  <title>Inverzzo - Únete a la Lista de Espera</title>
  <link rel="canonical" href="https://inverzzo.com">
</head>
<body class="bg-gray-50 font-sans text-gray-800">
  <!-- Hero Section -->
  <section class="min-h-screen bg-gradient-to-b from-blue-600 to-blue-800 flex flex-col justify-center items-center text-white px-4 py-12">
    <div class="text-center max-w-3xl">
      <h1 class="text-4xl sm:text-5xl font-bold mb-4">Inverzzo: Invierte con Confianza en Colombia</h1>
      <p class="text-lg sm:text-xl mb-8">La plataforma #1 para inversionistas minoristas. Accede a datos confiables, simulaciones de portafolios y educación financiera. ¡Sé el primero en unirte!</p>
      <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md mx-auto">
        <h2 class="text-xl font-semibold text-gray-800 mb-4">Únete a la Lista de Espera</h2>
        <form id="waitlist-form" class="space-y-4">
          <div>
            <input
              type="text"
              name="name"
              placeholder="Tu nombre"
              class="w-full p-3 rounded border border-gray-300 text-gray-800 focus:outline-none focus:ring-2 focus:ring-blue-500"
              required
            />
          </div>
          <div>
            <input
              type="email"
              name="email"
              placeholder="Tu correo electrónico"
              class="w-full p-3 rounded border border-gray-300 text-gray-800 focus:outline-none focus:ring-2 focus:ring-blue-500"
              required
            />
          </div>
          <button
            type="submit"
            class="w-full bg-blue-600 text-white p-3 rounded font-semibold hover:bg-blue-700 transition transform hover:scale-105"
          >
            Únete Ahora
          </button>
        </form>
        <p class="text-sm text-gray-600 mt-4">Únete a más de 1,000 inversionistas en la espera. ¡Sin spam, lo prometemos!</p>
      </div>
    </div>
  </section>

  <!-- Features Section -->
  <section class="py-12 px-4 sm:px-6 lg:px-8">
    <h2 class="text-3xl font-semibold text-center text-gray-800 mb-8">¿Por qué Inverzzo?</h2>
    <div class="grid grid-cols-1 sm:grid-cols-3 gap-6 max-w-5xl mx-auto">
      <div class="p-6 rounded-lg shadow-md bg-gray-50 text-center">
        <h3 class="text-lg font-semibold mb-2">Datos Confiables</h3>
        <p class="text-gray-600">Información en tiempo real sobre CDTs y acciones de la BVC, diseñada para Colombia.</p>
      </div>
      <div class="p-6 rounded-lg shadow-md bg-gray-50 text-center">
        <h3 class="text-lg font-semibold mb-2">Simulaciones de Portafolios</h3>
        <p class="text-gray-600">Prueba estrategias de inversión sin riesgos y visualiza tus resultados.</p>
      </div>
      <div class="p-6 rounded-lg shadow-md bg-gray-50 text-center">
        <h3 class="text-lg font-semibold mb-2">Educación Financiera</h3>
        <p class="text-gray-600">Blogs, videos y podcasts para tomar decisiones informadas.</p>
      </div>
    </div>
    <div class="text-center mt-8">
      <a href="#waitlist-form" class="inline-block bg-blue-600 text-white px-6 py-3 rounded font-semibold hover:bg-blue-700 transition">Regístrate Hoy</a>
    </div>
  </section>

  <Footer />

  <script>
    document.addEventListener('astro:page-load', () => {
      const form = document.getElementById('waitlist-form');
      if (form) {
        form.addEventListener('submit', (e) => {
          e.preventDefault();
          const name = e.target.elements.name.value;
          const email = e.target.elements.email.value;
          if (name && email) {
            alert(`¡Gracias, ${name}! Te has unido a la lista de espera de Inverzzo.`);
            console.log(`Nombre: ${name}, Email: ${email}`); // Simulate SES/CRM integration
            form.reset();
          } else {
            alert('Por favor, completa todos los campos.');
          }
        });
      }
    });
  </script>
</body>
</html>